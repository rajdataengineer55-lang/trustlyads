
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: These rules are insecure and for debugging only.
    // They allow anyone to upload, read, and delete any file.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
